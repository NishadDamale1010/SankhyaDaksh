import {useState} from 'react'; import {useNavigate} from 'react-router-dom'; import {useApp} from '../context/AppContext'; import {ShieldCheck,ArrowRight,LockKeyhole,Landmark} from 'lucide-react'
export default function Auth(){const [email,setEmail]=useState('officer@sankhyadaksh.local'),[password,setPassword]=useState('demo123'),[error,setError]=useState('');const {login}=useApp();const nav=useNavigate();const submit=e=>{e.preventDefault();const role=email.startsWith('admin')&&password==='admin123'?'admin':email.startsWith('officer')&&password==='demo123'?'officer':null;if(!role)return setError('Use one of the demonstration credentials shown below.');login(role);nav('/dashboard')};return <main className="login"><section className="login-story"><div className="login-logo"><Landmark/> SANKHYA-DAKSH</div><div className="eyebrow light">SIH 2026 · COMPETENCY INTELLIGENCE PLATFORM</div><h1>Make capability<br/>visible.</h1><p>From role requirement to demonstrated capability, Sankhya-Daksh identifies competency gaps and the next best learning action.</p><div className="loop"><span>Role requirement</span><i>→</i><span>Evidence</span><i>→</i><span>Gap</span><i>→</i><span>Next best action</span></div><div className="honest"><ShieldCheck size={17}/> Prototype / Local AI Engine · Local NSSTA & MoSPI-context datasets</div></section><section className="login-panel"><form onSubmit={submit}><div className="eyebrow">SECURE DEMO ACCESS</div><h2>Welcome back</h2><p>Sign in to view your competency intelligence.</p><label>Official email<input value={email} onChange={e=>setEmail(e.target.value)} /></label><label>Password<div className="password"><LockKeyhole size={17}/><input type="password" value={password} onChange={e=>setPassword(e.target.value)}/></div></label>{error&&<div className="login-error">{error}</div>}<button>Sign in securely <ArrowRight size={18}/></button></form><div className="demo-credentials"><b>Presentation credentials</b><div><span>OFFICER</span><code>officer@sankhyadaksh.local</code><code>demo123</code></div><div><span>ADMIN</span><code>admin@sankhyadaksh.local</code><code>admin123</code></div></div></section></main>}
